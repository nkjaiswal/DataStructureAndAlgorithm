/*IP: A huge file input.txt
        main.c
        main.cpp
        one.c
        one.cpp
        two.c
        three.cpp
        parse.py
        pasrse.css
        parse.html
        scripts.js
        random.cs
        .
        .
        .
        .
        .

    for() {
        int ext = null;
        File file = null;
        for(each element in the file){
            if( ext not null){
                if(element.ext == ext){
                    printFile element,file;
                    element.marked = true;
                }
            }
            if(ext == null && element does not marked){
                ext = element.ext;
                file = createFile( ext_prog_file)
                printFile element, file
                element.marked = true;
            }

        }
    }


    for(each element){
        ext = element.ext
        file = ext_prog_file if not exist then create
        file.append(element)
        file.close();
    }

        OP:
        c_prgo_files
        1_main.c
        2_one.c
        3_two.c


        cpp_prog_files
        1_main.cpp
        2_one.cpp
        three.cpp

        .
        .

        */
public class Oracle4 {
}
